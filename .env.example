# Copy to .env only if your shell tooling loads it, and never commit .env.
# qqa itself reads credentials from the process environment.
QQA_LLM_API_KEY=replace-with-your-api-key
QQA_LLM_BASE_URL=https://api.example.com
QQA_LLM_MODEL=your-model-id
